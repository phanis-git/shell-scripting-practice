# Environment variables
# export name="phani"      # we should not place in script instead we need to pass in cli
echo "Name before process kill :: $name"

# process_id=$(pgrep bash)
# echo "${process_id}"
# kill ${process_id}

# echo "Name after process kill :: $name"

